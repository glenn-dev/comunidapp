<%= form_with(model: expenses_detail, local: true) do |form| %>
  <% if expenses_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(expenses_detail.errors.count, "error") %> prohibited this expenses_detail from being saved:</h2>

      <ul>
      <% expenses_detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount %>
  </div>

  <div class="field">
    <%= form.label :bill_id %>
    <%= form.text_field :bill_id %>
  </div>

  <div class="field form-group col-md-8">
    <%= form.label :concept_id %>
    <%= form.select(:concept_id, options_for_select(@concept_array), prompt: 'Seleccione concepto') %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>


